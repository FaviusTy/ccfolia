import styled from "styled-components";
import {
  borders,
  display,
  flexbox,
  palette,
  positions,
  shadows,
  sizing,
  spacing,
  typography
} from "@material-ui/system";

const Box = styled.div`
${borders}
${display}
${flexbox}
${palette}
${positions}
${shadows}
${sizing}
${spacing}
${typography}
`;
export default Box;
