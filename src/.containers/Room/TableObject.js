const MapObject = ({ id, url, x, y, z, angle, locked, text }) => {};

const PieceObject = ({
  id,
  url,
  x,
  y,
  z,
  angle,
  name,
  status,
  text,
  secret
}) => {};

const SoundObject = ({
  id,
  url,
  x,
  y,
  z,
  angle,
  name,
  status,
  text,
  secret
}) => {};

// const DiceObject = ({  id, url, x, y, z, angle }) => {}
